# Global settings
RELEASE=trixie
DOCKER_ARMBIAN_BASE_IMAGE="ubuntu:noble"
INSTALL_HEADERS=yes
INSTALL_ARMBIAN_FIRMWARE=yes
BUILD_DESKTOP=no
BUILD_MINIMAL=yes
DEB_COMPRESS=xz
EXTRAWIFI=yes
ENABLE_EXTENSIONS="disable-bg-apt,fixed-eth-mac,perf-journald,perf-sysctl,perf-apt,perf-defaults,perf-fstab-noatime"

# Board-specific
BOARD=luckfox-pico-mini
#BRANCH=current
BRANCH=vendor
#BRANCH=edge
KERNEL_BTF=no
