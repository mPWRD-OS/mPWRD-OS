# Build the deck target from the Lyra Zero W config, then override only the
# board identity so it builds as a separate device.
source "${USERPATCHES_PATH}/config-luckfox-lyra-zero-w.conf"

BOARD=luckfox-lyra-zero-deck
