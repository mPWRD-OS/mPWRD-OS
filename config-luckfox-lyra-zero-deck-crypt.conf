# Build the encrypted deck target from the existing deck config, then enable
# build-time root encryption and touchscreen unlock support.
source "${USERPATCHES_PATH}/config-luckfox-lyra-zero-deck.conf"

ENABLE_EXTENSIONS="$ENABLE_EXTENSIONS,lyra-deck-unl0kr"
CRYPTROOT_ENABLE=yes
CRYPTROOT_SSH_UNLOCK=no
